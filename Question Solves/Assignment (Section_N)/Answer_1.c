#include <stdio.h>

int main() {
    int first,middle, last;
    printf("Enter three integers: ");
    scanf("%d %d %d", &first,& middle, &last);
    printf("First value = %d\n", first);
    printf("Last value = %d\n", last);
    return 0;
}
